setupAtmosphereVariants;
setupRefInputsVariants;
setupBballRefInputsVariants
setupEOMVariants;
setupDataOutVariants;
setupPubVariants;
setupBballPubVariants;
setupSubVariants;
setupRTPaceVariants;

% setupSensorVariants; % Currently, no Sensor Variants
% setupActuatorVariants; % Currently, no actuator variants
% setupPropulsionVariants; % Currently, no propulsion variants
% setupForceMomentVariants; % Currently, no Aero/Prop modeling variants
% setupTurbulenceVariants; ; % Currently, no turbulence variants
% setupControllerVariants; % Currently, no controller variants
